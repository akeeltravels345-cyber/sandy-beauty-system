# Static site - no build step. Netlify serves the repo root as-is.
[build]
  publish = "."

# Long-cache the versioned JS/CSS (they use ?v= cache-busting), never cache the HTML shell.
[[headers]]
  for = "/*.html"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
